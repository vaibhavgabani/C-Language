#include<stdio.h>
int main(){
    int size;
    printf("Enter Size :");
    scanf("%d",&size);
    int arr[size];
    //input
    for(int i = 0 ; i < size ; i++){
        printf("Enter at");
    }
    return 0;
}